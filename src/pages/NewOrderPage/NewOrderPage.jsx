import { useLocation } from 'react-router-dom';
import { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';

export default function NewOrderPage() {
  // const { parksData2, parkId } = useLocation().state;
  // console.log(parksData2, parkId, 'parks data and then the id' )
  // const park = parksData2.find(park => park.id === parkId);
  return (
    <div>
      {/* <div>{park.name}</div> */}
      <h1>Park Details</h1>
    </div>
  );
}